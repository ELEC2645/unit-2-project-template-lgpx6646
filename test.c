#include <stdio.h>
#include "funcs.h"   

int main(void) {

    return 0;
}
/*for (int i=0; i<*count; i++){
        printf("%lf %lf %lf %lf %lf %lf %lf\n",
                converters[i].Vin,
                converters[i].Vout,
                converters[i].L,
                converters[i].fs,
                converters[i].R,
                converters[i].deltav,
                converters[i].deltai
        );
        printf("\n");
    }*/